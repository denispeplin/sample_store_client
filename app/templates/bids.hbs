{{#each}}
<div {{bind-attr class=":panel placed:panel-success:panel-info"}}>
  <div class="panel-heading">
    {{format-date created_at}}
    {{view Ember.Checkbox checkedBinding="selected" class="checkbox-inline pull-right"}}
  </div>
  {{partial 'bid-products'}}
</div>
{{/each}}
