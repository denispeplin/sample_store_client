Invoices:

{{#each}}
  {{format-date created_at}}
  {{received}}

  {{#each product in invoiceProducts}}
    {{product.name}}
    {{product.amount}}
    {{product.price}}
  {{/each}}
{{/each}}

{{outlet}}
